# Vercel KV (Redis) Configuration
# These will be automatically set by Vercel when you connect KV storage
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# Optional: Enable test mode for deterministic time testing
# TEST_MODE=1
